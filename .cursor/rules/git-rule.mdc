---
description: Jacory Space Git 分支、提交和 PR 规范
alwaysApply: true
---

# git-rule

## Branch Naming

- 不在 `main` 上直接提交代码改动；先创建任务分支。
- 分支格式：`<type>/<short-kebab-desc>`。
- 使用完整分支前缀；不要用 `feat/` 作为分支前缀。
- 常用前缀：
  - `feature/`：新增功能或明显能力增强
  - `fix/`：修复 bug、异常、回归问题
  - `chore/`：脚本、配置、依赖、工具维护
  - `docs/`：只改文档或说明
- 示例：
  - `feature/browser-cookie-mode`
  - `fix/bilibili-parse-timeout`
  - `chore/dev-script`
  - `docs/agent-rules`

## Commit Message

- 使用一行提交信息。
- 格式：`<type>: 中文描述`。
- 常用类型：`feat`、`fix`、`chore`、`docs`。
- 示例：
  - `feat: 支持浏览器 Cookie 模式`
  - `fix: 修复哔哩哔哩解析失败`
  - `chore: 新增开发服务脚本`
  - `docs: 补充 Agent 协作规范`

## Workflow

- 提交前先看 `git status`，不要把本地运行时数据、下载文件、个人文档提交进仓库。
- 不提交 `jacory-space-backend/video-backend/data/users/`、`jacory-space-backend/video-backend/downloads/`、`.dev/`，除非用户明确要求。
- 用户说“提交”“提交这个分支”“commit”“submit this branch”时，默认含义是 **commit + push 当前分支**。
- 只有用户明确说“只提交”“只本地提交”“不要 push”时，才允许停在本地 commit。
- 每次成功 commit 后，如属于分支工作流，必须立即执行 `git push -u origin HEAD`；不要在 commit 和 push 之间结束回合，除非 push 失败或用户明确禁止 push。
- 创建 PR 时总结主要改动和验证方式。
- 如需提 PR：先 `git push -u origin HEAD`，再创建 PR，并把 PR 链接返回给用户手动检查。
- **禁止自动合并 PR**：Agent 不得自行执行 `gh pr merge`；只有用户明确发出“现在合并”指令后才可执行。
- PR 合并完成后切回 `main` 并 `git pull origin main`。
