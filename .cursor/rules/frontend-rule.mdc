---
description: Jacory Space 前端规范：图标用 icon 不用 emoji；样式优先 Tailwind 内联；字体/字号用命名档位
globs: jacory-space-frontend/**/*
alwaysApply: false
---

# frontend-rule

编辑 `jacory-space-frontend/` 时适用。

## 样式写法

- **默认**：样式直接用 Tailwind 工具类写在模板 `class` 上（颜色、间距、布局、定位、字体等）。
- **只在以下情况写 scoped `.xxx {}`**：
  1. 同一套样式被多个元素/组件**重复使用**，需要复用。
  2. Tailwind 难以表达的属性（复杂 `transform`、`letter-spacing`、伪元素、关键帧等）。
- **禁止**为「只用一次的几个工具类」单独包一个 `.xxx` 类。

## 图标

- **禁止**在 UI 里用 emoji 充当图标（如 🎬 📺 🔑 📁 ✓）
- **使用 icon**：SVG 组件或 icon 库（如 `@heroicons/vue`、`lucide-vue-next`）
- 按钮、导航、卡片装饰、状态提示等，一律走 icon

```vue
<!-- BAD -->
<button>🔑 Cookies 设置</button>
<span class="text-2xl">📺</span>

<!-- GOOD -->
<button>
  <KeyIcon class="w-4 h-4" />
  Cookies 设置
</button>
<MonitorPlayIcon class="w-6 h-6" />
```

- 新增 icon 依赖时，优先选轻量、与 Tailwind 搭配好的库；不要为单个图标手写大段 SVG，除非项目已有统一 icon 组件

## 字体 / 字号

- 字号用 Tailwind 命名档位（`text-xs` / `text-sm` / `text-base` …），按最接近的标准档选取。
- **禁止**任意值 `text-[0.78rem]` / `text-[11px]`，**禁止**在 `<style>` 写 `font-size`。
- 无 10px/11px 命名档，统一收敛到 `text-xs`；更显眼的层级用 `text-sm`。
- 字体族用 `font-sans`（正文/标题）、`font-mono`（编号/坐标/状态/meta）；**禁止** `<style>` 里 `font-family: theme("fontFamily.mono")`。
- `<style>` 只保留 Tailwind 难表达的属性（`letter-spacing`、`transform`、定位、`color` token、`text-transform` 等）；颜色用语义 token 类。

```vue
<!-- BAD -->
<span class="text-[11px]" style="font-size:0.78rem">{{ no }}</span>
<style scoped>
.label { font-family: theme("fontFamily.mono"); font-size: 0.625rem; }
</style>

<!-- GOOD -->
<span class="font-mono text-xs">{{ no }}</span>
<style scoped>
.label { letter-spacing: 0.16em; color: var(--muted-foreground); }
</style>
```
